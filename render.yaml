services:
  - type: web
    name: certamen-sourcebook-ai
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run main.py
    packages:
      - poppler-utils
      - tesseract-ocr
      - tesseract-ocr-eng
